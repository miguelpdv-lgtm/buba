
  # High-fidelity webpage design

  This is a code bundle for High-fidelity webpage design. The original project is available at https://www.figma.com/design/NYFQahMsgw5nApf30cgBJH/High-fidelity-webpage-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  